---
---
import LayoutDocs from "../../layouts/LayoutDocs.astro";
import CopyBlock from '../../components/CopyBlock.tsx';

<LayoutDocs active="/properties/onChange">
    # onChange

    Called during the update of the state of the chessboard.

    Returns the updated state of the chessboard in FEN notation, 
    indicating the move that was made and the piece that was moved.
    <br/>
    <h2 id="Typing" data-anchor>Typing</h2>
    <CopyBlock client:load>
        ```ts
        type MoveData = {
            FEN: string;
            from: [number, number];
            to: [number, number];
            figure: { 
                color: "white" | "black";
                type: "pawn" | "bishop" | "knigts" | "rook" | "queen" | "king";
            };
        }

        type onChange = (moveData: MoveData) => void;
        ```
    </CopyBlock>

    <br />
    <h2 id="Code" data-anchor>Code</h2>
    <CopyBlock client:load>
        ```tsx
        export const App = () => {
            const handleChange = (moveData) => {
                sendMove(moveData);
            }

            return (
                <ChessBoard 
                    FEN="rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq - 0 1"
                    onChange={handleChange}
                    onEndGame={() => {}}
                />
            );
        }
        ```
    </CopyBlock>
</LayoutDocs>