---
---
import LayoutDocs from '../../layouts/LayoutDocs.astro';
import { ChessBoardDefault } from '../../components/ChessBoards.tsx';
import CopyBlock from '../../components/CopyBlock.tsx';


<LayoutDocs active="/docs/quickStart">
    # Get started

    <h2 id="Install" data-anchor>Install</h2>
    <CopyBlock client:load>
        ```bash
        npm install react-chessboard-ui
        ```
    </CopyBlock>

    <br />
    <h2 id="Add in project" data-anchor>Add in project</h2>
    <CopyBlock client:load>
        ```tsx
        import type { FC } from "react";
        import { ChessBoard } from "react-chessboard-ui";

        export const App: FC = () => {
            return (
                <div className="app">
                    <ChessBoard 
                        FEN="rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq - 0 1"
                        onChange={(data) => {}}
                        onEndGame={(result) => {}}
                    />
                </div>
            );
        }
        ```
    </CopyBlock>

    Styles are injected automatically when the package is imported. The package adds one browser style tag with the id ```react-chessboard-ui-styles```.

    <br />
    <h2 id="Example" data-anchor>Example</h2>
    <div class="component-preview">
        <ChessBoardDefault client:only />
    </div>
</LayoutDocs>
